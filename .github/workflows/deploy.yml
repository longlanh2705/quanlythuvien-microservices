name: CI/CD Deploy Microservices

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy to Docker Compose
        run: |
          cd /home/sinhvien/quanlythuvien-microservices
          git pull origin master
          docker compose up -d --build
